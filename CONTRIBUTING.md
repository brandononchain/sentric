# Contributing to Sentric

Use Node 22.12+ and `npm ci`. Copy `.env.example` for local development, then run `npm test`. Include a regression test for parsing, scoring, payment or agent-protocol changes. Keep fixtures synthetic and label unknown data accurately. Do not add claims about wallet ownership, investment performance or provider latency without evidence. Open a pull request describing behavior, verification and any configuration migration.
